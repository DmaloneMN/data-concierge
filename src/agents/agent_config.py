"""Agent configuration loader."""
# TODO: Load agent config from environment / Key Vault

class AgentConfig:
    model_deployment: str = ""
    max_iterations: int = 10
    # TODO: Add remaining config fields
